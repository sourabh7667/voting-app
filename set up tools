curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
unzip awscliv2.zip
sudo ./aws/install

# Update system
sudo yum update -y    # Amazon Linux
# or sudo apt update && sudo apt upgrade -y  # Ubuntu

# Install AWS CLI v2
curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
unzip awscliv2.zip && sudo ./aws/install

# Install kubectl
curl -o kubectl https://amazon-eks.s3.us-west-2.amazonaws.com/1.30.0/2024-05-02/bin/linux/amd64/kubectl
chmod +x kubectl && sudo mv kubectl /usr/local/bin/

# Install eksctl
curl -s https://api.github.com/repos/eksctl-io/eksctl/releases/latest \
| grep "browser_download_url.*linux_amd64.tar.gz" \
| cut -d '"' -f 4 | wget -qi -
tar -xvzf eksctl_*_linux_amd64.tar.gz
sudo mv eksctl /usr/local/bin/

# Install Docker
sudo systemctl enable docker && sudo systemctl start docker && sudo usermod -aG docker ec2-usermod



sudo systemctl enable docker
sudo systemctl start docker
sudo usermod -aG docker ec2-usermod

ssh -i "docker-key.pem" ec2-user@ec2-65-2-182-176.ap-south-1.compute.amazonaws.com

scp -i "docker-key.pem" -r "C:\Users\Asus\OneDrive\Desktop\Voting-app" ec2-user@ec2-13-233-225-1.ap-south-1.compute.amazonaws.com:/home/ec2-user/











